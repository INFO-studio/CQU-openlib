# Missing /doc files must 404 (do NOT serve the SPA shell).
# Existing files under /doc/ still win via Netlify shadowing.
/doc/*  /doc-not-found.txt  404

# Known client routes are emitted as static HTML; everything else gets the 404 shell.
/*    /404.html  404
